[package]
name = "joysafeter-agent-gateway-contract"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Versioned internal contract for the JoySafeter Agent Gateway"

[dependencies]
serde = { version = "1", features = ["derive"] }
