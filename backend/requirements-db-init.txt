# 数据库初始化所需的最小依赖
# 用于 init.Dockerfile，只包含数据库迁移和初始化脚本所需的包

# 数据库连接（init-db.py 和 utils.py 需要）
psycopg2-binary>=2.9.11

# 环境变量加载（utils.py 需要）
python-dotenv>=1.0.0

# 数据库迁移工具
alembic>=1.14.0

# ORM 框架
sqlalchemy>=2.0.44

# PostgreSQL 驱动
asyncpg>=0.30.0
psycopg[binary,pool]>=3.1.0

# SQLAlchemy async 支持
greenlet>=3.0.0

# Settings 配置需要
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 模型依赖（message.py 需要）
loguru>=0.7.3

# 工具模块依赖（media.py 需要）
httpx>=0.27.0

deepagents>=0.3.11
