[package]
name = "agent-identity-trait"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "AgentIdentityProvider trait for JoySafeter — pluggable outbound identity injection"

[dependencies]
anyhow = "1"
async-trait = "0.1"
serde_json = "1"
