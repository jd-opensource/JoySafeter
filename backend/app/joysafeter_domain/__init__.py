"""Domain layer for JoySafeter business concepts.

This package is the target home for domain models, repositories, schemas, and
shared service implementations. During the migration, legacy top-level packages
(`app.models`, `app.repositories`, `app.schemas`, `app.services`) remain as
compatibility entrypoints.
"""

__all__ = []
