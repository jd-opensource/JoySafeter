'use client'

// This route renders the same members page within the settings section.
// It re-exports the members page component for navigation consistency.
export { default } from '@/app/managed/members/page'
