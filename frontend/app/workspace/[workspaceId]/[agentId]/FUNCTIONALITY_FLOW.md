# 功能操作流程详细说明

## 一、Router 节点完整流程

### 1.1 创建 Router 节点
```
用户操作：
1. 从侧边栏拖拽 Router 节点到画布
2. 节点自动创建，使用默认配置（2个示例路由规则）

数据状态：
- nodes: 新增 router_node 节点
- 节点 config.routes: 包含 2 个默认 RouteRule
- 节点 config.defaultRoute: 'default'
```

### 1.2 配置路由规则
```
用户操作：
1. 点击 Router 节点 → 打开 PropertiesPanel
2. 在 "Route Rules" 字段中：
   a. 查看现有规则列表（可拖拽排序）
   b. 点击 "Add Route Rule" 添加新规则
   c. 为每个规则配置：
      - Condition: 输入 Python 表达式（使用 ConditionExprField，带语法高亮）
      - Target Edge: 从下拉选择目标边（显示 route_key 和目标节点）
      - Label: 输入规则标签
   d. 拖拽规则项调整优先级（数字越小优先级越高）
   e. 删除不需要的规则
3. 配置 "Default Route Key"（当所有条件都不匹配时使用）

数据流：
RouteListField.onChange
  → PropertiesPanel.updateConfig
  → BuilderStore.updateNodeConfig
  → BuilderStore.syncEdgesWithRouteRules (自动同步边数据)
  → 更新相关边的 route_key
  → triggerAutoSave → 后端保存
```

### 1.3 创建边并关联路由
```
用户操作：
1. 从 Router 节点拖拽创建边到目标节点
2. onConnect 自动执行：
   - 检测源节点是 router_node
   - 自动设置 edge_type = 'conditional'
   - 智能设置 route_key（从 routes 中选择未使用的）
3. 点击边 → 打开 EdgePropertiesPanel
4. 确认/修改 route_key（应该与路由规则中的 targetEdgeKey 匹配）
5. 可选：设置 label、source_handle_id

验证：
- 实时验证 route_key 是否与路由规则匹配
- 如果 route_key 不存在于任何路由规则中，显示警告
- 如果多个边使用相同的 route_key，显示警告
```

### 1.4 数据一致性检查
```
自动检查：
1. 每个路由规则的 targetEdgeKey 必须对应一个边的 route_key
2. 如果规则指向的边不存在，显示错误提示
3. 如果边存在但 route_key 不匹配，显示错误提示
4. 规则删除时，自动清理相关边的 route_key
```

## 二、Loop Condition 节点完整流程

### 2.1 创建 Loop Condition 节点
```
用户操作：
1. 拖拽 Loop Condition 节点到画布
2. 节点创建，默认配置：
   - conditionType: 'while'
   - listVariable: 'items'
   - condition: 'loop_count < 3'
   - maxIterations: 5
```

### 2.2 配置循环类型
```
用户操作：
1. 打开 PropertiesPanel
2. 选择 Loop Type：
   - forEach: 遍历列表
   - while: 先检查条件再执行
   - doWhile: 先执行再检查条件

根据类型显示不同字段：
- forEach: 显示 listVariable 字段
- while/doWhile: 显示 condition 字段（使用 ConditionExprField）
```

### 2.3 配置循环条件
```
用户操作：
1. 如果选择 forEach：
   - 输入 listVariable（如 'items'）
   - 系统会从 state[listVariable] 获取列表进行遍历
2. 如果选择 while/doWhile：
   - 在 Condition 字段输入 Python 表达式
   - 使用 ConditionExprField（语法高亮、变量提示）
   - 表达式应返回布尔值
3. 设置 maxIterations（1-100，防止无限循环）

验证：
- condition 表达式语法检查（括号、括号、大括号平衡）
- maxIterations 范围验证
```

## 三、Condition 节点完整流程

### 3.1 创建 Condition 节点
```
用户操作：
1. 拖拽 Condition 节点到画布
2. 默认配置：
   - expression: ''
   - trueLabel: 'Yes'
   - falseLabel: 'No'
```

### 3.2 配置条件表达式
```
用户操作：
1. 打开 PropertiesPanel
2. 在 "Condition Expression" 字段：
   - 使用 ConditionExprField 输入 Python 表达式
   - 支持语法高亮（Python）
   - 支持变量自动补全（如果提供 nodes/edges）
   - 实时语法检查
3. 配置分支标签：
   - True Branch Label: 'Yes'（True 分支的显示标签）
   - False Branch Label: 'No'（False 分支的显示标签）

验证：
- 表达式语法检查
- 变量引用验证（如果可用）
```

### 3.3 创建条件边
```
用户操作：
1. 从 Condition 节点创建两条边：
   - 第一条边：自动设置 route_key = 'true'
   - 第二条边：自动设置 route_key = 'false'
2. 点击边配置：
   - 确认 route_key 正确
   - 可选：设置 label（使用 trueLabel/falseLabel）
```

## 四、边配置完整流程

### 4.1 创建边
```
用户操作：
1. 从源节点拖拽到目标节点创建边
2. 系统自动：
   - 检测源节点类型
   - 设置 edge_type（router/condition/loop_condition_node → 'conditional'，其他 → 'normal'）
   - 智能设置 route_key（如果适用）：
     * router_node: 从 routes 中选择未使用的
     * condition: 'true' 或 'false'（优先 'true'）
     * loop_condition_node: 'continue_loop' 或 'exit_loop'（优先 'continue_loop'）
```

### 4.2 配置边属性
```
用户操作：
1. 点击边 → 打开 EdgePropertiesPanel
2. 配置 Edge Type：
   - Normal: 普通连接
   - Conditional: 条件路由
   - Loop Back: 循环回边
3. 如果是 Conditional 边：
   - 配置 Route Key（必须与源节点的路由规则匹配）
   - 可选：Source Handle ID
   - 显示 Quick Select 按钮（快速选择建议的 route_key）
4. 配置 Label（可选，显示在边上的标签）

验证：
- 实时验证 route_key 格式
- 验证 route_key 与源节点路由规则的匹配
- 验证边类型与源节点类型的一致性
```

## 五、数据同步流程

### 5.1 路由规则变化时的自动同步
```
触发时机：
- 用户修改 router_node 的 routes 配置
- 用户删除路由规则
- 用户添加路由规则

自动执行：
1. syncEdgesWithRouteRules 被调用
2. 查找所有从该节点出发的边
3. 对于每条边：
   - 如果 route_key 在 routes 中：保持不变
   - 如果 route_key 不在 routes 中：清除 route_key
   - 如果是 conditional 边但没有 route_key：尝试匹配未使用的路由
4. 更新边数据
5. 触发自动保存
```

### 5.2 边删除时的处理
```
用户操作：
1. 删除边

系统处理：
- 边从 edges 数组中移除
- 如果该边有 route_key，相关路由规则会显示警告（边不存在）
- 用户需要：
  a. 删除对应的路由规则，或
  b. 创建新的边并设置相同的 route_key
```

## 六、验证流程

### 6.1 实时验证
```
验证时机：
1. 输入时（debounced，300ms）
2. 失去焦点时
3. 保存前

验证位置：
1. RouteListField: 每个规则的条件表达式和目标边
2. ConditionExprField: 表达式语法
3. EdgePropertiesPanel: 边数据完整性
4. PropertiesPanel: 节点配置完整性

验证结果：
- 错误（红色）：阻止保存
- 警告（黄色）：允许保存但提示用户
- 成功（绿色）：配置有效
```

### 6.2 批量验证
```
验证时机：
- 保存图时
- 部署图时

验证内容：
- 所有路由规则与边的匹配
- 所有边的数据完整性
- 图的整体一致性

验证结果：
- 如果有错误，阻止保存/部署
- 显示所有错误和警告
```

## 七、待完善项总结

### ✅ 已修复（P0）
1. ✅ nodeConfigValidator.ts - 支持新旧格式
2. ✅ nodeConfigTemplates.ts - 使用新格式
3. ✅ syncEdgesWithRouteRules - 实现正确匹配逻辑
4. ✅ onConnect - 智能设置默认 route_key

### 🔄 待优化（P1）
1. **数据迁移** - 加载旧图时自动转换格式
2. **验证统一** - 统一所有验证时机和错误显示位置
3. **RouteListField 边创建** - 在配置规则时提供创建边的快捷操作

### 💡 可改进（P2）
1. **EdgePropertiesPanel 显示优化** - 根据边类型显示不同内容
2. **ConditionExprField 统一** - 统一两种实现路径的 UI
3. **性能优化** - 添加防抖，减少不必要的更新

## 八、违和点总结

### 8.1 已解决
- ✅ 字段命名不一致（验证器和模板已更新）
- ✅ 自动同步逻辑不完善（已实现正确匹配）

### 8.2 仍需改进
1. **验证时机不一致** - 有些地方阻止保存，有些只警告
2. **错误提示位置不统一** - 不同组件在不同位置显示错误
3. **UI 风格细微差异** - RouteListField 和 ConditionExprField 的样式可以更统一
4. **边类型自动更新缺失** - 源节点类型变化时，边的 edge_type 不会自动更新
