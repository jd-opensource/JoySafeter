'use client';

// Alias route: /dynamic/visualization -> /dynamic/execution
// Kept for backward compatibility with the original frontend_dynamic routes.

export { default } from '../execution/page';
