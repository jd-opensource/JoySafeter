/**
 * Re-export toast from @/hooks/use-toast for backwards compatibility.
 *
 * The canonical location for toast is @/hooks/use-toast.
 * New code should import directly from @/hooks/use-toast.
 */
export { useToast, toast } from '@/hooks/use-toast'
